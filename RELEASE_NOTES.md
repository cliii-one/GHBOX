# 规划盒子

面向国土空间规划数据库的 ArcGIS Pro 批处理工具集。

## v1.0.4（2026-08）

- **新增 检查更新**：新增「帮助支持」分组。比对 GitHub 仓库最新版本与本机当前安装版本，存在新版本时提示，并可一键跳转 GitHub 下载页下载更新。

## v1.0.3（2026-08）

- **检查空洞**：修复多部件空洞只计 1 个的问题——工具链末尾增加「多部件转单部件」，每个空洞独立输出为一个面要素，数量与手动操作一致；融合改用成对融合（PairwiseDissolve），处理速度更快。
- **新增 按属性拆库**：从多个 GDB/MDB 中查找指定图层，按属性筛选条件批量导出为 SHP 或 GDB。
- **新增 按属性合并**：按指定属性字段合并相邻图斑，分组后将小图斑向相邻最大图斑合并，支持多字段组合。
- 数据库处理/检查全部 11 个工具统一窗口样式（GhBoxStyles.xaml）。

## 功能

### 数据库处理

| 工具 | 说明 |
|---|---|
| 数据库合并 | 将多个 GDB/MDB 中的同名图层合并到一个输出数据库 |
| 数据库拆分 | 按县代码与县名称将省级数据库拆分为县级独立 GDB |
| 按属性拆库 | 从多个 GDB/MDB 中查找指定图层，按属性筛选条件批量导出为 SHP 或 GDB |
| 按属性合并 | 按指定属性字段合并相邻图斑：分组后将小图斑向相邻最大图斑合并，支持多字段组合 |
| 数据库比对 | 比对两个版本数据库的图层名称、范围、几何与属性差异 |
| 唯一编码 | 按顺序给所选图层写入唯一编码 |
| 面积重算 | 按椭球面积（测地线）重算要素面积 |
| 删除图层 | 遍历根文件夹下所有 GDB/MDB，批量删除指定图层 |

### 数据库检查

| 工具 | 说明 |
|---|---|
| 查找弧线段 | 检查要素几何中的圆弧/椭圆弧/贝塞尔曲线段 |
| 查找尖锐角 | 检查要素几何中的尖锐角顶点（夹角小于阈值） |
| 检查空洞 | 检查面要素中的内环空洞（回字形/甜甜圈，内部中空）。工具链：融合（成对融合加速）→线→面→融合→擦除→拆单部件，每个空洞独立输出为一个面要素 |

### 帮助支持

| 工具 | 说明 |
|---|---|
| 检查更新 | 比对 GitHub 仓库最新版本与本机当前安装版本，发现新版本时可一键跳转 GitHub 下载页 |

## 安装

1. 从 [Releases](../../releases) 下载与你的 ArcGIS Pro 版本匹配的 `.esriAddInX`
2. 双击安装
3. 完全退出并重新启动 ArcGIS Pro
4. 功能区「规划盒子」选项卡

## 版本对照

| 安装包 | 适用 Pro 版本 | .NET |
|---|---|---|
| `GHBoxAddIn_Pro30.esriAddInX` | 3.0 ~ 3.2 | .NET 6 |
| `GHBoxAddIn_Pro33.esriAddInX` | 3.3 ~ 3.6 | .NET 8 |
| `GHBoxAddIn_Pro37.esriAddInX` | 3.7+ | .NET 10 |
