<controls:ProWindow x:Class="GHBoxAddIn.Scripts.GDB.MergeHelp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        mc:Ignorable="d"
        Title="数据库合并 - 使用说明" Height="560" Width="680"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="数据库合并工具" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

                <TextBlock Text="一、工具用途" FontSize="14" FontWeight="Bold" Margin="0,8,0,4" />
                <TextBlock TextWrapping="Wrap" Margin="8,0,0,4"
                           Text="将一个文件夹下多个 GDB/MDB 数据库中的同名图层，合并到一个已存在的输出数据库中。适合批量汇总各县（区）数据库成果的场景。" />

                <TextBlock Text="二、参数说明" FontSize="14" FontWeight="Bold" Margin="0,12,0,4" />
                <TextBlock Margin="8,0,0,2" Text="1. 需要合并的文件夹：包含多个 GDB/MDB 数据库的文件夹。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="2. 输出数据库：合并结果写入的数据库，必须是已创建好的 GDB 或 MDB。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="3. 需要合并的图层：多个图层名用英文逗号分隔（支持中文逗号）；输入 ALL 表示合并所有图层。" TextWrapping="Wrap" />

                <TextBlock Text="三、处理规则" FontSize="14" FontWeight="Bold" Margin="0,12,0,4" />
                <TextBlock Margin="8,0,0,2" Text="1. 自动枚举文件夹下全部 .gdb 和 .mdb，按名称排序后依次处理。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="2. 按图层名逐一合并：图层名不区分大小写，包含要素数据集内的图层。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="3. 某个数据库缺少该图层时，仅记录警告并跳过该库，不影响其他库。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="4. 所有数据库都没有该图层时，跳过该图层。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="5. 输出数据库中已存在同名图层时，先删除旧图层再合并。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="6. 合并后核对条数：如「库A条数 + 库B条数 = 合并后条数」，不一致仅提示警告，不中断处理。" TextWrapping="Wrap" />

                <TextBlock Text="四、容错机制" FontSize="14" FontWeight="Bold" Margin="0,12,0,4" />
                <TextBlock Margin="8,0,0,2" Text="单个图层合并失败时，记录到异常日志并继续处理下一个图层；全部处理结束后输出成功/失败统计和异常日志路径。" TextWrapping="Wrap" />
                <TextBlock Margin="8,0,0,2" Text="异常日志文件：输出数据库同级目录下的「数据库合并_异常日志_时间戳.txt」。" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</controls:ProWindow>
